[package]
name = "sync"
version = "0.1.0"
description = "Synchronisation primitive building blocks"
edition = "2021"

[dependencies.spin_rs]
package = "spin"
git = "https://github.com/tsoutsman/spin-rs"
branch = "rw-lock-guard-counts"
default-features = false
features = ["spin_mutex", "rwlock"]
