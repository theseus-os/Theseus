# if you encounter: 
# `error: no matching package named `bootloader` found`
# compile with -Z bindeps

[package]
name = "uefi_builder"
version = "0.1.0"
edition = "2021"
authors = ["Klim Tsoutsman <klim@tsoutsman.com>"]

[dependencies]
bootloader = { git = "https://github.com/theseus-os/bootloader", branch = "theseus" }
ovmf-prebuilt = "0.1.0-alpha.1"
