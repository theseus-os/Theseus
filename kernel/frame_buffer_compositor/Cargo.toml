[package]
name = "frame_buffer_compositor"
version = "0.1.0"
authors = ["Wenqiu Yu <yuwenqiuj@gmail.com>"]
description = "manage framebuffer objects and implement a compositor to composite multiple framebuffers"
build = "../../build.rs"

[dependencies]
spin = "0.4.10"
owning_ref = { git = "https://github.com/kevinaboos/owning-ref-rs" }

[dependencies.memory]
path = "../memory"

[dependencies.acpi]
path = "../acpi"

[dependencies.frame_buffer]
path = "../frame_buffer"

[dependencies.compositor]
path = "../compositor"

[dependencies.log]
default-features = false
version = "0.3.7"

# [build]
# rustflags = ["-C", "prefer-dynamic", "-C", "panic=abort"]

[lib]
crate-type = ["rlib"]
