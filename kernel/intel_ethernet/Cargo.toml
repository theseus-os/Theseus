[package]
name = "intel_ethernet"
version = "0.1.0"
description = "Code that is common for all Intel ethernet drivers"
authors = ["Ramla-I <ijazramla@gmail.com>"]
edition = "2018"

[dependencies]
volatile = "0.2.5"
bit_field = "0.7.0"

[dependencies.log]
default-features = false
version = "0.3.7"

[dependencies.memory]
path = "../memory"

[dependencies.network_interface_card]
path = "../network_interface_card"