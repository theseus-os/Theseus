[package]
name = "sync_block"
version = "0.1.0"
description = "Blocking synchronisation primitives"
edition = "2021"

[dependencies]
sync = { path = "../../libs/sync" }
sync_spin = { path = "../../libs/sync_spin" }
wait_queue = { path = "../wait_queue" }
