[package]
name = "test_avx"
version = "0.1.0"
authors = ["Min Hong Yun <mhyun@rice.edu>"]
build = "../../build.rs"

[dependencies]
x86_64 = { path = "../../libs/x86_64" } # currently using our local copy, forked from Phil Opp's crate
# packed_simd = "0.3.2"

[dependencies.log]
default-features = false
version = "0.3.7"

[lib]
crate-type = ["rlib"]

[dependencies.apic]
path = "../apic"

[dependencies.spawn]
path = "../spawn"

# [dependencies.scheduler]
# path = "../scheduler"