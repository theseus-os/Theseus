[package]
authors = ["Namitha Liyanage <namithaliyanage@gmail.com>"]
name = "scheduler_epoch"
description = "Provides epoch scheduling functionality and picks the next task"
version = "0.1.0"
edition = "2021"

[dependencies]
bit_set = { path = "../../libs/bit_set" }
kernel_config = { path = "../kernel_config" }
log = "0.4.8"
spin = "0.9.4"
task = { path = "../task" }

[lib]
crate-type = ["rlib"]
