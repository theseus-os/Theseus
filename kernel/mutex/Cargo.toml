[package]
name = "mutex"
version = "0.1.0"
authors = ["Klim Tsoutsman <klim@tsoutsman.com>"]
description = "A mutex implementation"
edition = "2021"

[dependencies]
scheduler = { path = "../scheduler" }
spin = "0.9.0"
task = { path = "../task" }
