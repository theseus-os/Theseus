[package]
authors = ["Kevin Boos <kevinaboos@gmail.com>"]
name = "per_cpu"
description = "Support for defining per-CPU data types"
version = "0.1.0"
edition = "2021"

[dependencies]
cpu = { path = "../cpu" }
cpu_local = { path = "../cpu_local" }
preemption = { path = "../preemption" }
task = { path = "../task" }
