[package]
authors = ["Wenqiu Yu <yuwenqiu@gmail.com>"]
name = "memory_interface"
description = "The virtual memory subsystem interface. It invokes arch-specific init function to initialize the memory subsystem."
version = "0.1.0"
build = "../../build.rs"

[dependencies]
memory = { path = "../memory" }
memory_x86 = { path = "../memory_x86" }

[dependencies.irq_safety]
git = "https://github.com/kevinaboos/irq_safety"

[lib]
crate-type = ["rlib"]
