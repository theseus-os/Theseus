[package]
name = "sync_preemption"
version = "0.1.0"
description = "Preemption-disabling synchronisation primitives"
edition = "2021"

[dependencies]
cpu_local_preemption = { path = "../cpu_local_preemption" }
sync = { path = "../../libs/sync" }
