[package]
name = "displayable"
version = "0.1.0"
authors = ["Wenqiu Yu <yuwenqiuj@gmail.com>"]
description = "a displayable is able to display itself in a framebuffer"
build = "../../build.rs"

[dependencies]
spin = "0.4.10"
downcast-rs = "1.0.4"

[dependencies.frame_buffer]
path = "../frame_buffer"

# [build]
# rustflags = ["-C", "prefer-dynamic", "-C", "panic=abort"]

[lib]
crate-type = ["rlib"]
