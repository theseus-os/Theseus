[package]
authors = ["Kevin Boos <kevinaboos@gmail.com>"]
name = "scheduler_round_robin"
description = "A basic round robin scheduler policy"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4.8"
spin = "0.9.4"

atomic_linked_list = { path = "../../libs/atomic_linked_list" }
cpu = { path = "../cpu" }
mutex_preemption = { path = "../mutex_preemption" }
runqueue_round_robin = { path = "../runqueue_round_robin" }
scheduler_policy = { path = "../scheduler_policy" }
task = { path = "../task" }
