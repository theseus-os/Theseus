[package]
name = "cls"
version = "0.1.0"
edition = "2021"

[dependencies]
cfg-if = "1.0.0"
cls-proc = { path = "cls-proc" }
cpu = { path = "../cpu"}
memory = { path = "../memory" }
sync_spin = { path = "../../libs/sync_spin" }

[target.'cfg(target_arch = "x86_64")'.dependencies]
x86_64 = "0.14.8"