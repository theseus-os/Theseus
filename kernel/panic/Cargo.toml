[package]
authors = ["Kevin Boos <kevinaboos@gmail.com>"]
name = "panic"
description = "Structs and simple routines for handling panic information and propagating panics"
version = "0.1.0"

[dependencies]


[dependencies.log]
default-features = false
version = "0.3.7"


[lib]
crate-type = ["rlib"] # "lib" does the same thing I think
