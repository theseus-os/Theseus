[package]
name = "waker"
version = "0.1.0"
authors = ["Kevin Boos <kevinaboos@gmail.com>", "Klim Tsoutsman <klim@tsoutsman.com>"]
description = "A standard Waker + Blocker implementation that directly uses task blocking"
edition = "2021"

[dependencies]
spin = "0.9.4"

task = { path = "../task" }
waker_generic = { path = "../waker_generic" }
