[package]
name = "dreadnought"
version = "0.1.0"
edition = "2021"

[dependencies]
async_channel = { path = "../async_channel" }
# TODO: MutexSleep
spin = "*"

[dependencies.futures]
version = "0.3"
default-features = false
features = ["alloc"]
