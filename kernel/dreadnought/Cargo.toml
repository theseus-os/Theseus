[package]
name = "dreadnought"
version = "0.1.0"
edition = "2021"

[dependencies]
scheduler = { path = "../scheduler" }
spin = "0.9"
task = { path = "../task" }

[dependencies.futures]
version = "0.3"
default-features = false
features = ["alloc"]
