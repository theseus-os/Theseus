[package]
authors = ["Jacob Earle <jacob.earle@yale.edu>"]
name = "scheduler_realtime"
description = "Provides realtime scheduling functionality and picks the next task according to the Rate Monotonic scheduling algorithm"
version = "0.1.0"
build = "../../build.rs"

[dependencies.log]
version = "0.4.8"

[dependencies.task]
path = "../task"

[dependencies.runqueue]
path = "../runqueue"

[dependencies.runqueue_realtime]
path = "../runqueue_realtime"

[lib]
crate-type = ["rlib"]
