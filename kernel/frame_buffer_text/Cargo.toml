[package]
name = "frame_buffer_text"
version = "0.1.0"
authors = ["snoword <qiuj@mail.bnu.edu.cn>"]

[dependencies]
spin = "0.4.5"
volatile = "0.1.0"


# [build]
# rustflags = ["-C", "prefer-dynamic", "-C", "panic=abort"]


[lib]
crate-type = ["rlib"] # "lib" does the same thing I think