[package]
name = "text_display"
version = "0.1.0"
authors = ["Andrew Pham <apham727@gmail.com>"]

[dependencies]
[dependencies.event_types]
path = "../../kernel/event_types"


[dependencies.log]
default-features = false
version = "0.3.7"

[lib]
crate-type = ["lib"] # "lib" does the same thing I think