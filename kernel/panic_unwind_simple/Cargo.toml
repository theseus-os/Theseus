[package]
authors = ["Kevin Boos <kevinaboos@gmail.com>"]
name = "panic_unwind_simple"
description = "Simple, minimalist entry points for panics and unwinding, as required by the Rust compiler."
version = "0.1.0"

[dependencies]

[dependencies.log]
default-features = false
version = "0.3.7"

[lib]
crate-type = ["rlib"]
