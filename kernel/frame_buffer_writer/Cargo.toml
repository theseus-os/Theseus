[package]
name = "frame_buffer_writer"
version = "0.1.0"
authors = ["Wenqiu Yu <yuwenqiuj@gmail.com>"]
description = "Basic crate to write a pixel to the framebuffer directly. Other display crates are based on this crate"
build = "../../build.rs"

[dependencies.frame_buffer]
path = "../frame_buffer"

# [build]
# rustflags = ["-C", "prefer-dynamic", "-C", "panic=abort"]


[lib]
crate-type = ["rlib"]
