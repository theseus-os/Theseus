[package]
name = "frame_buffer"
version = "0.1.0"
authors = ["Wenqiu Yu <yuwenqiuj@gmail.com>"]
description = "manage framebuffer objects. contain a final framebuffer which is mapped to the physical framebuffer"
build = "../../build.rs"

[dependencies]
spin = "0.4.10"
owning_ref = { git = "https://github.com/kevinaboos/owning-ref-rs" }

[dependencies.memory]
path = "../memory"

[dependencies.util]
path = "../../libs/util"

[dependencies.multicore_bringup]
path = "../multicore_bringup"

# [build]
# rustflags = ["-C", "prefer-dynamic", "-C", "panic=abort"]

[lib]
crate-type = ["rlib"]
