window.SIDEBAR_ITEMS = {"fn":[["get_current_time_in_ticks","Returns the current time in ticks"],["increment_tick_count","Update the current tick count Used as a callback in the systick handler"],["sleep","Blocks the current task by putting it to sleep for `duration` ticks."],["sleep_periodic","Blocks the current task for a fixed time `period`, which starts from the given `last_resume_time`."],["sleep_until","Blocks the current task by putting it to sleep until a specific tick count is reached, given by `resume_time`."],["unblock_sleeping_tasks","Remove all tasks that have been delayed but are able to be unblocked now, the current tick count is provided by the system’s interrupt tick count."]]};