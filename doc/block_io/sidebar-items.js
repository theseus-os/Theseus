initSidebarItems({"struct":[["BlockBounds","Block-wise bounds information for a data transfer (read or write) that has been calculated from a byte-wise offset and buffer length. "],["BlockIo","A wrapper around a `StorageDevice` that supports reads and writes of arbitrary byte lengths (down to a single byte) by issuing commands to the underlying storage device. This is needed because most storage devices only allow reads/writes of larger blocks,  e.g., a 512-byte sector or 4KB cluster."],["BlockSize","Information used to track the size of a block device. Primarily used to construct `BlockBounds` objects."]]});