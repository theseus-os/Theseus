window.SIDEBAR_ITEMS = {"struct":[["RxQueue","A struct that holds all information for one receive queue. There should be one such object per queue."],["TxQueue","A struct that holds all information for a transmit queue.  There should be one such object per queue."]],"trait":[["RxQueueRegisters","The register trait that gives access to only those registers required for receiving a packet. The Rx queue control registers can only be accessed by the physical NIC."],["TxQueueRegisters","The register trait that gives access to only those registers required for sending a packet. The Tx queue control registers can only be accessed by the physical NIC."]]};