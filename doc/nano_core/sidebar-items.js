window.SIDEBAR_ITEMS = {"macro":[["try_exit","Just like Rust’s `try!()` macro, but instead of performing an early return upon an error, it invokes the `shutdown()` function upon an error in order to cleanly exit Theseus OS."]]};